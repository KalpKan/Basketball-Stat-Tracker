# Mobile App

SwiftUI iPhone app for on-device shot capture and inference.

Planned modules:

- Camera capture using `AVFoundation`
- Shot detection and hoop-relative coordinate normalization using `Vision` or Core ML
- Event buffering and upload to Supabase

